[build-system]
requires = ["setuptools>=45", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "mcp-server-fastmcp"
version = "1.0.0"
description = "Mock package for mcp-server-fastmcp"